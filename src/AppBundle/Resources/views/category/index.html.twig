{% extends '@App/layout.html.twig' %}
{% block title %} Product list {% endblock %}
{% block body %}
<div class="starter-template">
    <h1>Product list</h1>

    {% for category in categories %}
    <a href="{{ path('product_by_category', {id: category.id}) }}">{{ category.name }}</a><br>
    {% endfor %}

</div>
{% endblock %}

{% block stylesheets %}

{% endblock %}
