{% extends '@App/layout.html.twig' %}
{% block title %} Product list {% endblock %}
{% block body %}
    <div class="starter-template">
        <h1>Product list</h1>

        {% for product in products %}
            <a href="{{ path('product_item', {id: product.id}) }}">{{ product.title }}</a><br>
        {% endfor %}

    </div>
{% endblock %}

{% block stylesheets %}

{% endblock %}
